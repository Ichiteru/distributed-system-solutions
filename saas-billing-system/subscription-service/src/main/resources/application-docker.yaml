SERVER:
  PORT: 8082

SPRING:
  APPLICATION:
    NAME: subscription-service
  DATASOURCE:
    URL: jdbc:postgresql://subscription-postgres:5432/subscription_service_db
    USERNAME: subscription_service_user
    PASSWORD: subscription_service_password
  KAFKA:
    BOOTSTRAP_SERVERS: kafka:9092
    PROPERTIES:
      SCHEMA:
        REGISTRY:
          URL: http://schema-registry:8081

SUBSCRIPTION:
  KAFKA:
    DLT:
      RETRY:
        BACKOFF_MS: 1000
        MAX_ATTEMPTS: 3
    TOPICS:
      COMMANDS:
        NAME: subscription.commands
      COMMANDS_DLT:
        NAME: subscription.commands.dlt
      EVENTS:
        NAME: subscription.events

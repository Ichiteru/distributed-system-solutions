spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/reactive_chat_service
    redis:
      host: localhost
      port: 6379

chat:
  # 0 means Runtime.availableProcessors()
  session-worker-pool-size: 0
